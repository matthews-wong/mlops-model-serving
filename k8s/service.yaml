apiVersion: v1
kind: Service
metadata:
  name: mlops-model-serving
  labels:
    app: mlops-model-serving
spec:
  type: ClusterIP
  selector:
    app: mlops-model-serving
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
